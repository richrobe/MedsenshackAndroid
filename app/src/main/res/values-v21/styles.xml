<resources>

    <style name="AppTheme" parent="@android:style/Theme.Material.Light">

        <!-- enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>

        <!-- specify enter and exit transitions -->
        <!--
        <item name="android:windowEnterTransition">@transition/slide</item>
        <item name="android:windowExitTransition">@transition/slide</item>
        -->
        <!-- Main theme colors -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/grey_800</item>
        <item name="android:editTextColor">@color/grey_800</item>

        <item name="android:itemTextAppearance">@style/myTheme.MenuText</item>
        <item name="android:actionBarTabTextStyle">@style/myTheme.ActionBarTabText.White</item>
    </style>


    <!-- Custom Option Menu Items -->
    <style name="myTheme.MenuText" parent="@android:style/TextAppearance.Widget.IconMenu.Item">
        <item name="android:textColor">@color/black</item>
        <item name="android:colorBackground">@color/grey_300</item>
    </style>

    <!-- Tab Text Color White -->
    <style name="myTheme.ActionBarTabText.White" parent="@android:style/Widget.ActionBar.TabText">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
